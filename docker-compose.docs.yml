services:
  docs:
    image: pms_api_local_docs
    container_name: pms_api_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./pms_api:/app/pms_api:z
    ports:
      - '9000:9000'
    command: /start-docs
